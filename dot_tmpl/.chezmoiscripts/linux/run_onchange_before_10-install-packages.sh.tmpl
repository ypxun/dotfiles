#!/bin/bash
# 监控清单变化: {{ include (joinPath .chezmoi.workingTree "packages/pkgs.txt") | sha256sum }}
set -eufo pipefail

{{ $sudo_val := "sudo " -}}
{{- if eq .chezmoi.username "root" -}}
    {{- $sudo_val = "" -}}
{{- end -}}

# 将 Go 变量的值赋给 Shell 变量 sudo
sudo={{ $sudo_val | quote }}
OS_ID={{ .osid | quote }}

# 1. 提取并翻译包清单
{{- $raw_pkgs := (executeTemplate (joinPath .chezmoi.workingTree "packages/pkgs.txt") . | splitList "\n") -}}
{{- $install_list := list -}}

{{- range $raw_pkgs -}}
    {{- $p := . | trim -}}
    {{- if and $p (not (hasPrefix "#" $p)) -}}
        {{- $target := $p -}}
        
        {{- /* 针对 Debian、Ubuntu 的特殊名翻译 */ -}}
        {{- if eq $.osid "debian" "ubuntu" -}}
            {{- if eq $p "fd" }}{{ $target = "fd-find" }}{{ end -}}
            {{- if or (eq $p "eza") (eq $p "micromamba") -}}
                {{- $target = "" }}
            {{- end -}}
        {{- end -}}
        
        {{- /* 针对 Arch 的名翻译 (Arch 源里 micromamba 通常叫 micromamba-bin 等，这里暂定原名或空) */ -}}
        {{- if eq $.osid "arch" -}}
            {{- if eq $p "micromamba" }}{{ end -}}
        {{- end -}}

        {{- if $target }}{{ $install_list = append $install_list $target }}{{ end -}}
    {{- end -}}
{{- end -}}

# 2. 执行安装
case "$OS_ID" in
    "debian"|"ubuntu")
        $sudo apt-get update
        INSTALL_CMD="$sudo apt-get install -y"
        ;;
    "arch")
        INSTALL_CMD="$sudo pacman -S --needed --noconfirm"
        ;;
esac

echo "📦 [1/3] 正在同步 $OS_ID 系统软件包..."
$INSTALL_CMD {{ $install_list | uniq | sortAlpha | join " " }}